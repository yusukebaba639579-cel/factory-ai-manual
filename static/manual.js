const video=document.querySelector('#video'),caption=document.querySelector('#caption');
const steps=[...document.querySelectorAll('.step')],rows=[...document.querySelectorAll('.gantt-row')];
const duration=Number(document.querySelector('.gantt').dataset.duration)||1,playhead=document.querySelector('#playhead');
const playButton=document.querySelector('#play-toggle'),seek=document.querySelector('#seek');
let selected=null;
function timeLabel(value){const minute=Math.floor(value/60),second=Math.floor(value%60);return `${minute}:${String(second).padStart(2,'0')}`}
function activeRanges(step){try{const ranges=JSON.parse(step.dataset.active||'[]');if(Array.isArray(ranges)&&ranges.length)return ranges.map(item=>({start:Number(item[0]),end:Number(item[1])}))}catch(error){}return[{start:Number(step.dataset.start),end:Number(step.dataset.end)}]}
function applyFocus(step){const rotated=Number(video.dataset.rotation)===180;video.style.setProperty('--focus-x',rotated?'50%':`${Number(step.dataset.focusX||.5)*100}%`);video.style.setProperty('--focus-y',rotated?'50%':`${Number(step.dataset.focusY||.5)*100}%`)}
function alignTimelineTracks(){const body=document.querySelector('.gantt-body'),scale=document.querySelector('.gantt-scale');if(!body||!rows.length)return;const seekRect=seek.getBoundingClientRect(),bodyRect=body.getBoundingClientRect(),thumbRadius=9;const left=Math.max(0,seekRect.left+thumbRadius-bodyRect.left),right=Math.max(0,bodyRect.right-(seekRect.right-thumbRadius)),gap=10;rows.forEach(row=>{row.style.paddingLeft='0';row.style.paddingRight='0';row.style.gridTemplateColumns=`${Math.max(0,left-gap)}px minmax(0,1fr) ${Math.max(0,right-gap)}px`});if(scale){scale.style.marginLeft=`${left}px`;scale.style.marginRight=`${right}px`}syncPosition()}
function syncPosition(value=video.currentTime){const current=Math.max(0,Math.min(duration,Number(value)||0));seek.value=current;document.querySelector('#current-time').textContent=timeLabel(current);const track=rows[0]?.querySelector('.gantt-track');const body=document.querySelector('.gantt-body');if(track&&body){const trackRect=track.getBoundingClientRect(),bodyRect=body.getBoundingClientRect();playhead.style.left=`${trackRect.left-bodyRect.left+Math.min(1,current/duration)*trackRect.width}px`}}
function selectProcess(index,autoplay=true){const step=steps[index];if(!step)return;const active=activeRanges(step);selected={index,start:active[0].start,end:active[active.length-1].end,active};steps.forEach((item,i)=>item.classList.toggle('active',i===index));rows.forEach((item,i)=>item.classList.toggle('active',i===index));applyFocus(step);video.currentTime=selected.start;syncPosition(selected.start);caption.textContent=step.querySelector('p').textContent;document.querySelector('#number').textContent=index+1;document.querySelector('#title').textContent=step.querySelector('b').textContent;document.querySelector('#description').textContent=step.querySelector('p').textContent;document.querySelector('#duration').textContent=step.querySelector('small').textContent;if(autoplay)video.play().catch(()=>{})}
steps.forEach((item,index)=>item.onclick=()=>selectProcess(index));rows.forEach((item,index)=>item.onclick=()=>selectProcess(index));
playButton.onclick=()=>video.paused?video.play():video.pause();
video.addEventListener('play',()=>playButton.textContent='❚❚ 一時停止');video.addEventListener('pause',()=>playButton.textContent='▶ 再生');
seek.oninput=()=>{video.currentTime=Number(seek.value);syncPosition(seek.value)};
document.querySelector('#playback-speed').onchange=event=>{video.playbackRate=Number(event.currentTarget.value)};
function setRotation(value){video.dataset.rotation=String(value);video.style.setProperty('--manual-rotation',`${value}deg`);if(selected)applyFocus(steps[selected.index])}setRotation(Number(video.dataset.rotation)||0);
document.querySelector('#flip-toggle').onclick=event=>{setRotation((Number(video.dataset.rotation)+180)%360);event.currentTarget.classList.toggle('active')};
video.addEventListener('timeupdate',()=>{syncPosition();if(!selected)return;const next=selected.active.find(range=>range.start>video.currentTime+.04);const inside=selected.active.some(range=>video.currentTime>=range.start-.04&&video.currentTime<range.end-.04);if(!inside&&next){video.currentTime=next.start;syncPosition(next.start);return}if(video.currentTime>=selected.end-.08){video.currentTime=selected.start;syncPosition(selected.start);if(!video.paused)video.play().catch(()=>{})}});
window.addEventListener('resize',()=>requestAnimationFrame(alignTimelineTracks));
new ResizeObserver(()=>requestAnimationFrame(alignTimelineTracks)).observe(document.querySelector('.playback-controls'));
video.addEventListener('loadedmetadata',alignTimelineTracks);
video.addEventListener('ended',()=>{if(selected){video.currentTime=selected.start;video.play().catch(()=>{})}});if(steps.length)selectProcess(0,false);requestAnimationFrame(alignTimelineTracks);
